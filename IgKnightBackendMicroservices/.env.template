# IgKnight Chess - Environment Variables Template
# 
# ⚠️ WARNING: DO NOT COMMIT THIS FILE WITH REAL SECRETS! ⚠️
# This is a TEMPLATE for documentation purposes only.
# Copy this file, fill in real values, and keep it LOCAL ONLY.
#
# For production: Use proper secret management (Vault, AWS Secrets Manager, K8s Secrets)
#

# =============================================================================
# CRITICAL - REQUIRED FOR PRODUCTION (No defaults)
# =============================================================================

# JWT Secret - MUST be unique per environment
# Generate with: openssl rand -base64 32
# REQUIRED BY: auth-service, api-gateway, realtime-game-service
JWT_SECRET=CHANGE_ME_IN_PRODUCTION

# Google OAuth Credentials - Get from Google Cloud Console
# REQUIRED BY: auth-service
GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# =============================================================================
# DATABASE - Override for production (has insecure dev defaults)
# =============================================================================

# Database URL
# auth-service default: jdbc:sqlserver://localhost:1433;databaseName=IgKnightauthservicedb;encrypt=true;trustServerCertificate=true
# game-service default: jdbc:sqlserver://localhost:1433;databaseName=IgKnightgameservicedb;encrypt=true;trustServerCertificate=true
# user-profile-service default: jdbc:sqlserver://localhost:1433;databaseName=IgKnightuserservicedb;encrypt=true;trustServerCertificate=true
DB_URL=jdbc:sqlserver://localhost:1433;databaseName=YourDatabaseName;encrypt=true;trustServerCertificate=true

# Database Credentials
# NEVER use dev defaults (Tejdeep/Tejdeep@100) in production!
DB_USERNAME=your_db_username
DB_PASSWORD=your_db_password

# =============================================================================
# URLS - Override for production (has localhost defaults)
# =============================================================================

# Frontend URL (where users access the application)
# Default: http://localhost:5173
# Production example: https://igknight.com
FRONTEND_URL=http://localhost:5173

# OAuth Redirect URI (Google OAuth callback)
# Must match what's configured in Google Cloud Console
# Default: http://localhost:8083/api/auth/login/oauth2/code/google
# Production example: https://api.igknight.com/api/auth/login/oauth2/code/google
OAUTH_REDIRECT_URI=http://localhost:8083/api/auth/login/oauth2/code/google

# CORS Allowed Origins (comma-separated, NO spaces)
# Controls CORS for:
#   - api-gateway: All REST API endpoints
#   - realtime-game-service: WebSocket /ws/game
#   - game-service: STOMP WebSocket /ws/chess
# Default: http://localhost:5173,http://localhost:3000,http://localhost:8080
# Production example: https://igknight.com,https://www.igknight.com
# ⚠️ CRITICAL: Use https:// for production, no trailing slashes, no spaces
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:8080

# =============================================================================
# MICROSERVICE URLs - Override for production (has localhost defaults)
# =============================================================================

# Game Service URL
# Used by: api-gateway, matchmaking-service, realtime-game-service
# Default: http://localhost:8082
# Production: http://game-service:8082 (internal DNS/service discovery)
GAME_SERVICE_URL=http://localhost:8082

# Auth Service URL
# Used by: api-gateway
# Default: http://localhost:8084
AUTH_SERVICE_URL=http://localhost:8084

# User Profile Service URL
# Used by: api-gateway
# Default: http://localhost:8085
USER_PROFILE_SERVICE_URL=http://localhost:8085

# Matchmaking Service URL
# Used by: api-gateway
# Default: http://localhost:8086
MATCHMAKING_SERVICE_URL=http://localhost:8086

# =============================================================================
# OPTIONAL - Has reasonable defaults
# =============================================================================

# JWT Token Expiration (milliseconds)
# Default: 86400000 (24 hours)
JWT_EXPIRATION=86400000

# =============================================================================
# LOCAL DEVELOPMENT VALUES (for reference)
# =============================================================================
# 
# For local development, you can use these values:
#
# JWT_SECRET=lCRPHrPH8VBO8YTlgpPfOz9fLef4UfPpI2F3bZ8Czzk
# DB_USERNAME=Tejdeep
# DB_PASSWORD=Tejdeep@100
# GOOGLE_CLIENT_ID=<get from Google Cloud Console>
# GOOGLE_CLIENT_SECRET=<get from Google Cloud Console>
#
# But REMEMBER: These are hardcoded in application.properties as defaults.
# You only need to set environment variables if you want to OVERRIDE the defaults.
#

# =============================================================================
# PRODUCTION CHECKLIST
# =============================================================================
#
# Before deploying to production:
#
# [ ] Generate NEW JWT_SECRET: openssl rand -base64 32
# [ ] Create production Google OAuth credentials
# [ ] Set strong DB_PASSWORD: openssl rand -base64 32
# [ ] Update FRONTEND_URL to production domain
# [ ] Update OAUTH_REDIRECT_URI to production API URL
# [ ] Update CORS_ALLOWED_ORIGINS to production domain(s)
# [ ] Update all service URLs to internal network addresses
# [ ] NEVER use localhost in production!
# [ ] NEVER commit this file with real secrets!
#
