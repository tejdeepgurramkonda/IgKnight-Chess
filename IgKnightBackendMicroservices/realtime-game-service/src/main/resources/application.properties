spring.application.name=igknight-realtime-game-service
server.port=8087

# Game Service configuration
game.service.url=http://localhost:8082

# JWT Configuration (must match auth-service and api-gateway)
jwt.secret=
jwt.expiration=86400000

# CORS Configuration for WebSocket
cors.allowed.origins=http://localhost:5173,http://localhost:3000,http://localhost:8080

